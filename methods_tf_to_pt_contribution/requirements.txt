# Core (PyTorch + NumPy + HDF5 for weight files)
torch>=2.0
numpy>=1.23
h5py>=3.8

# TensorFlow reference graph (parity vs Keras .h5)
tensorflow>=2.14
keras>=3.0
silence-tensorflow>=1.2
absl-py

# Dataset-scale benchmark (optional)
seisbench>=0.5
tqdm>=4.65
