"""
TensorFlow → PyTorch weight transfer and validation for EQCCT-style pickers.

This package is a **standalone copy** of the conversion and validation code from the
parent project. Import as ``eqcct_tf_pt_transfer`` after adding ``src/`` to
``PYTHONPATH`` (see the bundle ``README.md``).
"""

__version__ = "1.0.0"
